[build]
command = "npm run build"
publish = "build"

# 리다이렉트 규칙
[[redirects]]
from = "/sitemap.xml"
to = "/sitemap.xml"
status = 200
force = true

# 기본 리다이렉트 규칙
[[redirects]]
from = "/*"
to = "/index.html"
status = 200
